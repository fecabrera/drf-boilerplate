name: Docker Image CI

on:
  push:
    branches:
      - main # Triggers the workflow on pushes to the main branch
  pull_request: # Triggers the workflow on pull requests

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Build Docker image
      uses: docker/build-push-action@v6.16.0
      with:
        context: .
        file: Dockerfile
        tags: app-test:${{ github.sha }}

    # Testing and coverage steps
    - name: Run tests with coverage report
      run: |
        docker run --rm app-test:${{ github.sha }} pipenv install --dev --deploy --system
        docker run --rm app-test:${{ github.sha }} coverage run manage.py test
        docker run --rm app-test:${{ github.sha }} coverage report
